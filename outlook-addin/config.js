/**
 * URL base da API da intranet (sem barra final).
 * Ajuste para o ambiente de produção antes do deploy.
 */
window.ASSINATURA_ADDIN_CONFIG = {
  apiBaseUrl: 'https://intranet.wtorre.com.br/api/v1',
};
